import PlayerStats from '@components/default/PlayerStats';
import type { NextPage } from 'next';

const PlayerStatsPage: NextPage = () => {
  return <PlayerStats />;
};

export default PlayerStatsPage;